# Scheduler Validation

The implementation is validated by `tests/test_ppi_public_migration_scheduler.py`, Python compilation, read-only workflow permissions, one active task, SHA-pinned external actions, and explicit rejection of secrets, provider collection, private dispatch, write permissions, and automatic merge authority.
