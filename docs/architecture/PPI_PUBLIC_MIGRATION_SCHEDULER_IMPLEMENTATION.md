# Initial Scheduler Implementation

The initial implementation contains:

- one phase-controlled task queue;
- one read-only readiness reporter;
- one weekday public workflow;
- boundary tests;
- SHA-pinned checkout and artifact actions;
- a 14-day readiness artifact;
- no write or downstream authority.
