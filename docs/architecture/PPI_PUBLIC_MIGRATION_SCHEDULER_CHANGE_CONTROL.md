# PPI Scheduler Change Control

Only a reviewed pull request may:

- mark the active task complete;
- activate a different task;
- change the schedule;
- expand repository access;
- add secrets;
- add write permissions; or
- authorize a downstream action.

The initial scheduler has no such authority.
