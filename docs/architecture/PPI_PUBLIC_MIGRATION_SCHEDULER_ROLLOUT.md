# PPI Scheduler Rollout

The scheduler starts with `T01` only. Later tasks require one focused implementation pull request and one reviewed configuration update each. The scheduler never advances the queue by itself.
