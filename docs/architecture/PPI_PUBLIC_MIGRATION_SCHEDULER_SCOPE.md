# PPI Scheduler Scope

The scheduler performs read-only public readiness checks for one active migration task. It has no authority to implement, merge, dispatch, collect, score, register, publish, or trade.
