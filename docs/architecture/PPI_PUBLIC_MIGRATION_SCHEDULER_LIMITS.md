# Scheduler Limits

This scheduler is limited to one weekday read-only readiness report for the active migration task. It cannot modify code, advance tasks, collect provider data, access private workflows, merge pull requests, or mutate the registry.
