# Scheduler Ownership Boundary

`ai-market-news` owns the public migration scheduler. `ppi-data-acquisition` owns provider execution. `ai-signal-engine` owns final private analysis. The scheduler cannot assume either downstream role.
